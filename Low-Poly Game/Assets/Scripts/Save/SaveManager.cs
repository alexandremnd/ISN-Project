using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Newtonsoft.Json;
using System.IO;

// Créer un fichier JSON dans le dossier Documents du système. Conserve le nom de la sauvegarde, les succès, 
// temps de jeu et chemin vers la sauvegarde.
public class SaveManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
